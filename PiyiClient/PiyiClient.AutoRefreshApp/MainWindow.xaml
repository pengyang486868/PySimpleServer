<Window x:Class="PiyiClient.AutoRefreshApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PiyiClient.AutoRefreshApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0" Text="{Binding ServerAddress}" Width="250" HorizontalAlignment="Left"/>
        <Label Grid.Row="1" Content="{Binding HeardWords}"/>
        <Button Grid.Row="2" Content="click" Click="ButtonBase_OnClick" Width="120" HorizontalAlignment="Left"/>

    </Grid>
</Window>
